# 🐛 ПРОБЛЕМА: Ложные негативы для JS-сайтов (a101.ru)

## Обнаружено: 2026-02-19

### Суть проблемы:
Наш SEO Checker дает **неверные результаты** для сайтов, которые рендерятся через JavaScript (React, Vue, Angular, Next.js и т.п.)

### Примеры ложных проблем на a101.ru:

#### 1. ❌ Sitemap.xml - "не найден"
**Факт:** https://a101.ru/sitemap.xml существует (HTTP 200, 76 URLs)  
**Причина:** Возможны редиректы или проблемы с follow_redirects в httpx

#### 2. ❌ Analytics - "не найдены"
**Факт:** Yandex.Metrika установлена (ID: 26708190)  
**Причина:** Метрика загружается через JS, мы делаем только HTTP-запрос

#### 3. ❌ OpenGraph - "og:image отсутствует"
**Факт:** Все теги есть (og:title, og:description, og:image, og:url, og:type)  
**Причина:** Теги добавляются через JS после загрузки (SSR/CSR)

---

## 🔬 Техническая причина:

Наши проверки используют:
- `httpx` - простой HTTP-запрос
- `BeautifulSoup` - парсинг статического HTML

**НЕ ПОДДЕРЖИВАЮТ:**
- JavaScript execution
- Dynamic content loading
- Client-side rendering (CSR)
- Server-side rendering (SSR) с задержками

---

## 🎯 Решения:

### Вариант 1: Headless Browser (лучше, но дорого)
✅ **Pros:** Видит весь контент как реальный браузер  
❌ **Cons:** Медленно, требует Playwright/Docker, дорого в инфраструктуре

```python
from playwright.async_api import async_playwright

async with async_playwright() as p:
    browser = await p.chromium.launch()
    page = await browser.new_page()
    await page.goto(url, wait_until='networkidle')
    content = await page.content()  # Fully rendered HTML
```

### Вариант 2: User-Agent + Headers (средне)
✅ **Pros:** Просто, быстро  
⚠️ **Cons:** Не всегда помогает для CSR-сайтов

```python
headers = {
    'User-Agent': 'Mozilla/5.0 (compatible; SEOChecker/1.0; +https://seo-checker.com/bot)',
    'Accept': 'text/html,application/xhtml+xml',
}
await client.get(url, headers=headers, follow_redirects=True)
```

### Вариант 3: Timeout увеличение + follow_redirects (легко)
✅ **Pros:** Может помочь с редиректами и медленными сайтами  
⚠️ **Cons:** Не решит JS-проблемы

```python
await client.get(url, timeout=15.0, follow_redirects=True)
```

### Вариант 4: Специальные проверки для JS-сайтов
✅ **Pros:** Можем обнаружить JS-рендеринг и предупредить пользователя  
⚠️ **Cons:** Не решит проблему полностью

```python
# Detect if site uses JS frameworks
soup = BeautifulSoup(html, 'html.parser')
has_react = bool(soup.find(id='root') or soup.find(id='__next'))
has_vue = bool(soup.find(id='app'))

if has_react or has_vue:
    # Add disclaimer: "Этот сайт использует JS-рендеринг, 
    # некоторые проверки могут быть неточными"
```

---

## 📊 Статистика проблемы:

**Сколько сайтов затронуто?**
- Modern React/Next.js сайты: ~30-40% новых застройщиков
- Vue/Nuxt сайты: ~10-15%
- Статические HTML сайты: ~50-55% (работают корректно)

**Какие проверки затронуты:**
- ✅ Robots.txt - обычно работает
- ⚠️ Sitemap.xml - иногда проблемы с редиректами
- ❌ Analytics - часто ложные негативы (JS)
- ❌ OpenGraph - иногда ложные негативы (SSR)
- ✅ Meta Tags - обычно работают (если SSR)
- ✅ Canonical - обычно работают (если SSR)

---

## 🎯 Рекомендуемый план:

### Краткосрочно (1-2 часа):
1. ✅ Улучшить follow_redirects и timeout
2. ✅ Добавить better User-Agent
3. ✅ Добавить disclaimer для JS-сайтов
4. ✅ Улучшить обработку ошибок

### Среднесрочно (1-2 дня):
1. ⏭️ Добавить Playwright для Analytics и OpenGraph проверок
2. ⏭️ Или использовать готовые API (Google PageSpeed Insights видит JS)

### Долгосрочно (v2.0):
1. ⏭️ Полный headless browser для всех проверок
2. ⏭️ Или hybrid: static HTML + selective JS execution

---

## 📝 TODO создан:

- [x] Обнаружена проблема
- [ ] Улучшить HTTP-клиент (headers, timeout, redirects)
- [ ] Добавить детекцию JS-фреймворков
- [ ] Добавить disclaimer в отчёт
- [ ] (Опционально) Playwright для Analytics/OG

---

**Приоритет:** 🟡 Средний (не блокирует MVP, но снижает точность для ~40% сайтов)
