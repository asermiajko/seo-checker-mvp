# Спецификация проверок SEO для инструмента

## Метаданные

**Источник:** статья `seo-basics-for-developer-site.md`  
**Дата:** 2026-02-18  
**Назначение:** автоматическая проверка базового SEO сайтов застройщиков

---

## Структура проверок

Каждая проверка имеет:
- **ID** — уникальный идентификатор
- **Название** — короткое описание для пользователя
- **Критичность** — 🔴 критично / 🟡 важно / 🟢 усиление
- **Метод проверки** — как технически проверить
- **Статус** — ✅ OK / ⚠️ частично / ❌ проблема
- **Рекомендация** — что делать, если проблема обнаружена
- **Автоматизируемость** — можно ли проверить автоматически без API

---

## 1. Фундамент: Техническая база 🔴

### 1.1. Robots.txt

**ID:** `tech-robots`  
**Критичность:** 🔴 критично  
**Автоматизируемость:** ✅ полная

**Что проверять:**
1. Файл `robots.txt` существует (GET `/robots.txt` → статус 200)
2. Содержит директиву `User-agent: *`
3. Содержит ссылку на `Sitemap:` с валидным URL
4. Закрывает служебные разделы (`Disallow: /admin/`, параметры фильтров)

**Метод проверки:**
```python
response = requests.get(f"{site_url}/robots.txt")
if response.status_code == 200:
    content = response.text
    has_user_agent = "User-agent:" in content
    has_sitemap = "Sitemap:" in content
    # Извлечь URL sitemap для проверки 1.2
```

**Оценка:**
- ✅ OK: файл есть, содержит User-agent и Sitemap
- ⚠️ частично: файл есть, но отсутствует Sitemap или User-agent
- ❌ проблема: файл отсутствует (404) или пустой

**Рекомендация при проблеме:**
```
❌ Robots.txt отсутствует или неполный

Что это значит: Поисковые роботы не понимают, какие страницы индексировать.

Как исправить:
• Создайте файл /robots.txt в корне сайта
• Добавьте директивы User-agent: * и Sitemap: https://ваш-сайт.ru/sitemap.xml
• В Ida.Lite: это настраивается автоматически в разделе "SEO → Robots.txt"
```

---

### 1.2. Sitemap.xml

**ID:** `tech-sitemap`  
**Критичность:** 🔴 критично  
**Автоматизируемость:** ✅ полная

**Что проверять:**
1. Файл `sitemap.xml` доступен (GET → 200)
2. Валидный XML (можно распарсить)
3. Содержит URL-ы (проверка наличия тега `<url>`)
4. URL-ы внутри sitemap доступны (выборочно проверить 2-3 URL → 200)

**Метод проверки:**
```python
import xml.etree.ElementTree as ET

sitemap_url = f"{site_url}/sitemap.xml"
response = requests.get(sitemap_url)
if response.status_code == 200:
    try:
        root = ET.fromstring(response.content)
        urls = root.findall('.//{http://www.sitemaps.org/schemas/sitemap/0.9}url')
        url_count = len(urls)
    except:
        # Невалидный XML
```

**Оценка:**
- ✅ OK: sitemap доступен, валидный, содержит URL-ы
- ⚠️ частично: sitemap есть, но пустой или с ошибками
- ❌ проблема: sitemap отсутствует (404)

**Рекомендация при проблеме:**
```
❌ Sitemap.xml отсутствует или невалиден

Что это значит: Поисковики не видят список ваших страниц.

Как исправить:
• Создайте файл sitemap.xml со списком всех важных страниц (ЖК, квартиры)
• В Ida.Lite: генерируется автоматически, доступен по адресу /sitemap.xml
```

---

### 1.3. Скорость загрузки

**ID:** `tech-speed`  
**Критичность:** 🔴 критично  
**Автоматизируемость:** ⚠️ частичная (нужен API или headless browser)

**Что проверять:**
1. Время полной загрузки главной страницы < 3 сек
2. Размер HTML < 500 КБ
3. Количество запросов < 100
4. Наличие сжатия (gzip/brotli)

**Метод проверки (упрощённый):**
```python
import time
start = time.time()
response = requests.get(site_url)
load_time = time.time() - start

html_size = len(response.content) / 1024  # в КБ
has_compression = 'gzip' in response.headers.get('Content-Encoding', '')
```

**Примечание:** Для полной проверки нужен Google PageSpeed Insights API или Lighthouse CLI.

**Оценка:**
- ✅ OK: < 3 сек, размер оптимален
- ⚠️ частично: 3-5 сек или размер 500-1000 КБ
- ❌ проблема: > 5 сек или размер > 1 МБ

**Рекомендация при проблеме:**
```
⚠️ Сайт загружается медленно (5.2 сек)

Что это значит: 40% пользователей уйдут до загрузки, поисковики снизят позиции.

Как исправить:
• Оптимизируйте изображения (сжатие, lazy loading)
• Включите кэширование
• Минифицируйте CSS/JS
• В Ida.Lite: автоматическая оптимизация изображений под устройства
```

---

### 1.4. HTML-карта сайта

**ID:** `tech-html-sitemap`  
**Критичность:** 🔴 критично  
**Автоматизируемость:** ✅ полная

**Что проверять:**
1. Страница `/sitemap/` существует (GET → 200)
2. Содержит ссылки (наличие тегов `<a href=...>`)
3. Ссылка на HTML-карту есть в футере главной страницы

**Метод проверки:**
```python
from bs4 import BeautifulSoup

# Проверка наличия /sitemap/
sitemap_response = requests.get(f"{site_url}/sitemap/")
has_sitemap_page = sitemap_response.status_code == 200

# Проверка ссылки в футере
main_page = requests.get(site_url)
soup = BeautifulSoup(main_page.content, 'html.parser')
footer = soup.find('footer')
sitemap_link_in_footer = footer and ('/sitemap' in str(footer) or 'карта сайта' in str(footer).lower())
```

**Оценка:**
- ✅ OK: HTML-карта есть, ссылка в футере
- ⚠️ частично: HTML-карта есть, но нет ссылки в футере
- ❌ проблема: HTML-карта отсутствует

**Рекомендация при проблеме:**
```
❌ HTML-карта сайта отсутствует

Что это значит: Пользователям и поисковикам сложнее ориентироваться.

Как исправить:
• Создайте страницу /sitemap/ со ссылками на все разделы
• Добавьте ссылку на карту в футер
• В Ida.Lite: HTML-карта генерируется автоматически
```

---

### 1.5. Индексация (noindex)

**ID:** `tech-indexing`  
**Критичность:** 🔴 критично  
**Автоматизируемость:** ✅ полная

**Что проверять:**
1. На главной странице НЕТ тега `<meta name="robots" content="noindex">`
2. На главной странице НЕТ `<meta name="robots" content="nofollow">`

**Метод проверки:**
```python
soup = BeautifulSoup(response.content, 'html.parser')
robots_meta = soup.find('meta', attrs={'name': 'robots'})

if robots_meta:
    content = robots_meta.get('content', '').lower()
    has_noindex = 'noindex' in content
    has_nofollow = 'nofollow' in content
```

**Оценка:**
- ✅ OK: нет noindex/nofollow
- ❌ проблема: есть noindex или nofollow на главной

**Рекомендация при проблеме:**
```
❌ Главная страница закрыта от индексации (noindex)

Что это значит: Поисковики НЕ индексируют ваш сайт. Вы не попадёте в выдачу!

Как исправить:
• СРОЧНО удалите тег <meta name="robots" content="noindex"> с главной страницы
• Это тестовая настройка, которую забыли убрать перед запуском
```

---

## 2. Каркас: Структура и контент 🔴🟡

### 2.1. ЧПУ (человекопонятные URL)

**ID:** `content-seo-urls`  
**Критичность:** 🔴 критично  
**Автоматизируемость:** ⚠️ частичная (нужен анализ внутренних ссылок)

**Что проверять:**
1. URL главной страницы не содержит параметров (`?id=`, `?page=`)
2. URL содержит читаемые слова (кириллица/латиница), а не цифры/хеши
3. Внутренние ссылки используют ЧПУ

**Метод проверки:**
```python
import re

def is_seo_friendly(url):
    # Проверка на параметры
    if '?' in url and ('id=' in url or 'page=' in url or 'cat=' in url):
        return False
    
    # Проверка на читаемость (есть буквы, не только цифры)
    path = url.split('?')[0]  # убрать query string
    has_letters = bool(re.search(r'[a-zа-яё]', path, re.IGNORECASE))
    
    return has_letters

# Проверить главную + 3-5 внутренних ссылок
```

**Оценка:**
- ✅ OK: URL читаемые, без параметров
- ⚠️ частично: часть URL с параметрами
- ❌ проблема: все URL с параметрами (`?id=123`)

**Рекомендация при проблеме:**
```
❌ URL не оптимизированы (параметры вместо ЧПУ)

Что это значит: В выдаче вместо "example.ru/zhk-solnechnyj" показывается "example.ru?id=123"

Как исправить:
• Настройте ЧПУ (человекопонятные URL) для всех страниц
• Пример: /projects/zhk-solnechnyj/ вместо /page.php?id=123
• В Ida.Lite: ЧПУ настроены из коробки для всех разделов
```

---

### 2.2. Title и Description

**ID:** `content-meta-tags`  
**Критичность:** 🔴 критично  
**Автоматизируемость:** ✅ полная

**Что проверять:**
1. Наличие тега `<title>`
2. Длина title: 50-70 символов (оптимально)
3. Наличие `<meta name="description">`
4. Длина description: 120-160 символов (оптимально)
5. Title не дублирует H1 полностью
6. Title содержит конкретику (цены, название ЖК, УТП)

**Метод проверки:**
```python
title = soup.find('title')
title_text = title.text.strip() if title else ""
title_length = len(title_text)

description_meta = soup.find('meta', attrs={'name': 'description'})
description_text = description_meta.get('content', '').strip() if description_meta else ""
description_length = len(description_text)

h1 = soup.find('h1')
h1_text = h1.text.strip() if h1 else ""
title_duplicates_h1 = (title_text == h1_text)

# Проверка конкретики (цены, цифры)
has_numbers = bool(re.search(r'\d', title_text))
```

**Оценка:**
- ✅ OK: title и description заполнены, длина оптимальна, есть конкретика
- ⚠️ частично: есть, но слишком короткие/длинные или нет конкретики
- ❌ проблема: title или description отсутствуют

**Рекомендация при проблеме:**
```
❌ Title отсутствует или неоптимален

Текущий title: "Главная" (7 символов)

Что это значит: В выдаче ваш сайт выглядит неинформативно, CTR будет низким.

Как исправить:
• Заполните title конкретными данными: название ЖК, цены, УТП
• Пример: "ЖК Солнечный — квартиры от 3,2 млн ₽ | Официальный сайт застройщика"
• В Ida.Lite: шаблоны title с автоподстановкой параметров (цены, названия)
```

---

### 2.3. H1 и структура заголовков

**ID:** `content-headings`  
**Критичность:** 🔴 критично  
**Автоматизируемость:** ✅ полная

**Что проверять:**
1. Наличие тега `<h1>` на странице
2. Только ОДИН `<h1>` на странице
3. H1 не пустой (содержит текст)
4. Есть заголовки `<h2>` (структура контента)
5. Порядок заголовков логичный (H1 → H2 → H3, без пропусков)

**Метод проверки:**
```python
h1_tags = soup.find_all('h1')
h1_count = len(h1_tags)
h1_text = h1_tags[0].text.strip() if h1_tags else ""

h2_tags = soup.find_all('h2')
h2_count = len(h2_tags)

# Проверка порядка заголовков
all_headings = soup.find_all(['h1', 'h2', 'h3', 'h4', 'h5', 'h6'])
heading_levels = [int(h.name[1]) for h in all_headings]
is_logical_order = all(heading_levels[i] <= heading_levels[i+1] + 1 for i in range(len(heading_levels)-1))
```

**Оценка:**
- ✅ OK: один H1, есть H2, порядок логичный
- ⚠️ частично: H1 есть, но несколько на странице или нет H2
- ❌ проблема: H1 отсутствует

**Рекомендация при проблеме:**
```
❌ H1 отсутствует или некорректен

Найдено H1: 3 шт. (должен быть 1)

Что это значит: Поисковики не понимают главную тему страницы.

Как исправить:
• Установите один уникальный H1 на каждой странице
• Пример для главной ЖК: "ЖК Солнечный — квартиры от застройщика ПИК"
• Используйте H2-H3 для подразделов (планировки, инфраструктура)
```

---

### 2.4. Внутренняя перелинковка

**ID:** `content-internal-links`  
**Критичность:** 🟡 важно  
**Автоматизируемость:** ✅ полная

**Что проверять:**
1. Количество внутренних ссылок на главной странице
2. Наличие анкоров (текст ссылок не "Подробнее" / "Читать")
3. Ссылки ведут на страницы сайта (не внешние)

**Метод проверки:**
```python
from urllib.parse import urlparse

links = soup.find_all('a', href=True)
internal_links = []

site_domain = urlparse(site_url).netloc

for link in links:
    href = link['href']
    link_domain = urlparse(href).netloc
    
    # Внутренняя ссылка: относительный путь или тот же домен
    if not link_domain or link_domain == site_domain:
        internal_links.append({
            'url': href,
            'text': link.text.strip()
        })

internal_count = len(internal_links)
generic_anchors = [l for l in internal_links if l['text'].lower() in ['подробнее', 'читать', 'далее', 'ссылка']]
```

**Оценка:**
- ✅ OK: >= 10 внутренних ссылок, анкоры информативные
- ⚠️ частично: 5-10 ссылок или много "Подробнее"
- ❌ проблема: < 5 внутренних ссылок

**Рекомендация при проблеме:**
```
⚠️ Слабая внутренняя перелинковка (5 ссылок)

Что это значит: Поисковики и пользователи не видят связь между разделами.

Как исправить:
• Добавьте блоки "Похожие ЖК", "Другие квартиры в районе"
• Используйте информативные анкоры: "Квартиры в Химках" вместо "Подробнее"
• В Ida.Lite: блоки перелинковки настраиваются в конструкторе
```

---

### 2.5. Фильтровые страницы (коммерческие запросы)

**ID:** `content-filter-pages`  
**Критичность:** 🟡 важно  
**Автоматизируемость:** ⚠️ частичная (нужно знать структуру сайта)

**Что проверять:**
1. Наличие страниц типа `/kvartiry-2k/`, `/studii/` (ЧПУ фильтров)
2. Проверка нескольких популярных фильтров (1к, 2к, студии, с отделкой)
3. Страницы фильтров имеют уникальные title/description

**Метод проверки:**
```python
# Проверить наличие типовых фильтровых страниц
filter_urls = [
    f"{site_url}/kvartiry-1k/",
    f"{site_url}/kvartiry-2k/",
    f"{site_url}/studii/",
    f"{site_url}/s-otdelkoj/"
]

filter_pages_found = []
for url in filter_urls:
    response = requests.get(url)
    if response.status_code == 200:
        filter_pages_found.append(url)
```

**Оценка:**
- ✅ OK: >= 3 фильтровых страниц с ЧПУ
- ⚠️ частично: 1-2 фильтровых страницы
- ❌ проблема: фильтры отсутствуют или используют параметры (`?filter=2k`)

**Рекомендация при проблеме:**
```
⚠️ Фильтровые страницы под коммерческие запросы отсутствуют

Что это значит: Запросы "купить 2к квартиру" не ранжируются, агрегаторы забирают трафик.

Как исправить:
• Создайте отдельные страницы с ЧПУ: /kvartiry-2k/, /studii/, /s-otdelkoj/
• Каждая страница должна иметь уникальный title/description под запрос
• В Ida.Lite: автогенерация фильтровых страниц под ключевые запросы
```

---

## 3. Отделка: Микроразметка и сниппеты 🟡

### 3.1. Микроразметка Schema.org

**ID:** `meta-schema`  
**Критичность:** 🟡 важно  
**Автоматизируемость:** ✅ полная

**Что проверять:**
1. Наличие JSON-LD блока `<script type="application/ld+json">`
2. Тип разметки: `ApartmentComplex`, `Product`, `Organization`, `Breadcrumb`
3. Валидность JSON

**Метод проверки:**
```python
import json

schema_scripts = soup.find_all('script', type='application/ld+json')
schemas_found = []

for script in schema_scripts:
    try:
        schema_data = json.loads(script.string)
        schema_type = schema_data.get('@type', '')
        schemas_found.append(schema_type)
    except:
        pass  # Невалидный JSON

has_schema = len(schemas_found) > 0
```

**Оценка:**
- ✅ OK: есть микроразметка (минимум 1 тип)
- ⚠️ частично: разметка есть, но неполная
- ❌ проблема: микроразметка отсутствует

**Рекомендация при проблеме:**
```
⚠️ Микроразметка Schema.org отсутствует

Что это значит: В выдаче не будет расширенного сниппета (цена, адрес, фото).

Как исправить:
• Добавьте JSON-LD разметку: ApartmentComplex для ЖК, Product для квартир
• Это увеличивает CTR на 20-30%
• В Ida.Lite: автоматическая генерация микроразметки для всех типов страниц
```

---

### 3.2. OpenGraph (для соцсетей)

**ID:** `meta-opengraph`  
**Критичность:** 🟡 важно  
**Автоматизируемость:** ✅ полная

**Что проверять:**
1. Наличие `<meta property="og:title">`
2. Наличие `<meta property="og:description">`
3. Наличие `<meta property="og:image">`
4. Размер изображения (идеально 1200×630 px, проверить через URL)

**Метод проверки:**
```python
og_title = soup.find('meta', property='og:title')
og_description = soup.find('meta', property='og:description')
og_image = soup.find('meta', property='og:image')

has_og = all([og_title, og_description, og_image])

if og_image:
    og_image_url = og_image.get('content', '')
    # Опционально: проверить размер изображения через HEAD-запрос
```

**Оценка:**
- ✅ OK: все теги OpenGraph заполнены
- ⚠️ частично: часть тегов отсутствует
- ❌ проблема: OpenGraph отсутствует полностью

**Рекомендация при проблеме:**
```
⚠️ OpenGraph теги отсутствуют

Что это значит: При публикации в соцсетях ссылка выглядит неинформативно (нет превью).

Как исправить:
• Добавьте теги og:title, og:description, og:image
• Изображение: 1200×630 px, до 300 КБ
• В Ida.Lite: настройка OpenGraph для каждой страницы в админке
```

---

## 4. Инженерия: Работа с дублями 🔴

### 4.1. Canonical (канонические ссылки)

**ID:** `duplicates-canonical`  
**Критичность:** 🔴 критично  
**Автоматизируемость:** ✅ полная

**Что проверять:**
1. Наличие тега `<link rel="canonical" href="...">`
2. URL в canonical соответствует текущему URL (без параметров)

**Метод проверки:**
```python
canonical = soup.find('link', rel='canonical')

if canonical:
    canonical_url = canonical.get('href', '')
    # Сравнить с текущим URL
    current_url_clean = site_url.split('?')[0]  # без параметров
    is_correct = canonical_url.startswith(current_url_clean)
```

**Оценка:**
- ✅ OK: canonical есть и корректен
- ⚠️ частично: canonical есть, но указывает на другую страницу
- 🟢 не критично: canonical отсутствует, но нет параметров в URL (главная страница)

**Рекомендация при проблеме:**
```
⚠️ Canonical тег отсутствует

Что это значит: Если на сайте есть фильтры/сортировки, поисковики будут индексировать дубли.

Как исправить:
• Добавьте <link rel="canonical"> на страницы с параметрами (?sort=, ?filter=)
• Canonical должен указывать на основную версию страницы
• В Ida.Lite: canonical добавляется автоматически на все фильтры
```

---

### 4.2. Закрытие технических URL

**ID:** `duplicates-disallow`  
**Критичность:** 🔴 критично  
**Автоматизируемость:** ⚠️ частичная (через robots.txt)

**Что проверять:**
1. В `robots.txt` закрыты параметры фильтров: `Disallow: /*?sort=`, `Disallow: /*?filter=`
2. Закрыты админки: `Disallow: /admin/`, `Disallow: /wp-admin/`

**Метод проверки:**
```python
# Из проверки 1.1 (robots.txt)
has_disallow_params = bool(re.search(r'Disallow:.*\?', robots_content))
has_disallow_admin = '/admin' in robots_content.lower() or '/wp-admin' in robots_content.lower()
```

**Оценка:**
- ✅ OK: параметры и админка закрыты
- ⚠️ частично: закрыта только админка
- ❌ проблема: ничего не закрыто

**Рекомендация при проблеме:**
```
❌ Служебные URL не закрыты от индексации

Что это значит: Поисковики индексируют дубли и технические страницы.

Как исправить:
• Добавьте в robots.txt:
  Disallow: /admin/
  Disallow: /*?sort=
  Disallow: /*?filter=
• В Ida.Lite: настройка исключений в разделе "SEO → Robots.txt"
```

---

### 4.3. Пагинация (rel="next" / rel="prev")

**ID:** `duplicates-pagination`  
**Критичность:** 🟡 важно (для Яндекса)  
**Автоматизируемость:** ⚠️ частичная (нужно найти страницу со 2+ страницами)

**Что проверять:**
1. На страницах каталога (если есть пагинация) присутствует `<link rel="next">` или `<link rel="prev">`

**Метод проверки:**
```python
# Попробовать найти страницу с пагинацией (например /kvartiry/?page=2)
pagination_url = f"{site_url}/kvartiry/?page=2"
response = requests.get(pagination_url)

if response.status_code == 200:
    soup_page = BeautifulSoup(response.content, 'html.parser')
    rel_prev = soup_page.find('link', rel='prev')
    rel_next = soup_page.find('link', rel='next')
    
    has_pagination_tags = rel_prev or rel_next
```

**Оценка:**
- ✅ OK: пагинация размечена
- 🟢 не применимо: пагинации нет (все на одной странице)
- ⚠️ частично: пагинация есть, но не размечена

**Рекомендация при проблеме:**
```
⚠️ Пагинация не размечена (rel=next/prev)

Что это значит: Яндекс хуже понимает связь между страницами каталога.

Как исправить:
• Добавьте теги <link rel="next"> и <link rel="prev"> на страницы каталога
• В Ida.Lite: пагинация размечается автоматически
```

---

## 5. Прописка: Локальное SEO 🟡

### 5.1. Яндекс.Карты и локальные упоминания

**ID:** `local-maps`  
**Критичность:** 🟡 важно  
**Автоматизируемость:** ⚠️ ручная (нужна проверка через API Яндекс.Карт или вручную)

**Что проверять:**
1. Упоминание района/города в контенте главной страницы
2. Упоминание метро (если релевантно)
3. Наличие карты на странице (iframe Яндекс.Карты или 2GIS)

**Метод проверки:**
```python
page_text = soup.get_text().lower()

# Поиск упоминаний локации (примерный список)
locations = ['москва', 'спб', 'санкт-петербург', 'химки', 'мытищи', 'метро']
has_location_mention = any(loc in page_text for loc in locations)

# Поиск встроенной карты
has_map = bool(soup.find('iframe', src=lambda x: x and ('yandex.ru/map' in x or '2gis' in x)))
```

**Оценка:**
- ✅ OK: есть упоминания локации + карта
- ⚠️ частично: есть упоминания, но нет карты
- ❌ проблема: нет упоминаний локации

**Рекомендация при проблеме:**
```
⚠️ Отсутствуют локальные упоминания

Что это значит: Сайт хуже ранжируется по запросам "новостройки [район]".

Как исправить:
• Добавьте в описание: район, ближайшее метро, город
• Встройте карту Яндекс.Карты с меткой ЖК
• В Ida.Lite: интеграция с Яндекс.Картами в один клик
```

---

## 6. Коммуникации: Инструменты и аналитика 🔴

### 6.1. Счётчики аналитики

**ID:** `analytics-counters`  
**Критичность:** 🔴 критично  
**Автоматизируемость:** ✅ полная

**Что проверять:**
1. Наличие Яндекс.Метрики (скрипт `mc.yandex.ru/metrika`)
2. Наличие Google Analytics (скрипт `google-analytics.com/analytics.js` или `googletagmanager.com/gtag/js`)

**Метод проверки:**
```python
html_content = str(soup)

has_yandex_metrika = 'mc.yandex.ru/metrika' in html_content or 'metrika/tag.js' in html_content
has_google_analytics = 'google-analytics.com' in html_content or 'googletagmanager.com' in html_content
```

**Оценка:**
- ✅ OK: установлены оба счётчика
- ⚠️ частично: установлен один счётчик
- ❌ проблема: счётчики отсутствуют

**Рекомендация при проблеме:**
```
❌ Счётчики аналитики не установлены

Что это значит: Вы не видите, откуда приходят пользователи, что делают на сайте.

Как исправить:
• Установите Яндекс.Метрику (обязательно для РФ)
• Установите Google Analytics 4
• В Ida.Lite: подключение через копирование ID счётчика в админке
```

---

## 7. Дополнительные проверки (бонус)

### 7.1. Favicon

**ID:** `bonus-favicon`  
**Критичность:** 🟢 усиление  
**Автоматизируемость:** ✅ полная

**Что проверять:**
1. Наличие `<link rel="icon">` или `<link rel="shortcut icon">`
2. Файл `/favicon.ico` доступен (GET → 200)

**Метод проверки:**
```python
favicon_link = soup.find('link', rel=['icon', 'shortcut icon'])
has_favicon_tag = favicon_link is not None

favicon_file = requests.get(f"{site_url}/favicon.ico")
has_favicon_file = favicon_file.status_code == 200
```

**Оценка:**
- ✅ OK: favicon установлен
- ⚠️ проблема: favicon отсутствует

**Рекомендация:**
```
⚠️ Favicon отсутствует

Что это значит: В закладках браузера и вкладках нет иконки сайта.

Как исправить:
• Добавьте файл favicon.ico в корень сайта
• Или добавьте <link rel="icon" href="/favicon.png">
```

---

### 7.2. SSL-сертификат (HTTPS)

**ID:** `bonus-ssl`  
**Критичность:** 🔴 критично  
**Автоматизируемость:** ✅ полная

**Что проверять:**
1. Сайт доступен по HTTPS
2. Редирект с HTTP на HTTPS

**Метод проверки:**
```python
is_https = site_url.startswith('https://')

# Проверка редиректа
http_url = site_url.replace('https://', 'http://')
response_http = requests.get(http_url, allow_redirects=False)
redirects_to_https = response_http.status_code in [301, 302] and 'https://' in response_http.headers.get('Location', '')
```

**Оценка:**
- ✅ OK: HTTPS включен, редирект настроен
- ⚠️ частично: HTTPS работает, но нет редиректа с HTTP
- ❌ проблема: сайт без HTTPS

**Рекомендация при проблеме:**
```
❌ HTTPS не настроен

Что это значит: Google снижает позиции сайтов без SSL. Браузеры показывают "Небезопасно".

Как исправить:
• Установите SSL-сертификат (бесплатно через Let's Encrypt)
• Настройте редирект с HTTP на HTTPS
• В Ida.Lite: SSL включается автоматически при публикации сайта
```

---

## Итоговая структура отчёта

После всех проверок пользователь получает:

### Скор (общая оценка)
```
SEO-скор вашего сайта: 6.5 / 10

🔴 Критичные проблемы: 3
🟡 Важные доработки: 5
🟢 Рекомендации: 2
```

### Разбивка по категориям
```
1. Техническая база: 5/10 🔴
2. Структура и контент: 7/10 🟡
3. Микроразметка: 4/10 🟡
4. Работа с дублями: 8/10 ✅
5. Локальное SEO: 6/10 🟡
6. Аналитика: 3/10 🔴
```

### Топ-3 приоритетных исправлений
```
1. ❌ СРОЧНО: Главная страница закрыта от индексации (noindex)
2. ❌ КРИТИЧНО: Счётчики аналитики не установлены
3. ⚠️ ВАЖНО: Title и Description не оптимизированы
```

### Детальный отчёт
Каждая проверка с описанием + рекомендацией (см. выше).

### CTA в конце
```
Хорошая новость: все эти проблемы решаются за 1 час в конструкторе Ida.Lite!

👉 Попробовать Ida.Lite бесплатно
👉 Записаться на демо с разбором вашего сайта
```

---

## Технические требования к реализации

### Архитектура
1. **Веб-форма** — пользователь вводит URL сайта
2. **Парсер** (Python) — выполняет все проверки
3. **Telegram-бот** — отправляет результат в TG

### Стек
- **Backend:** Python 3.10+
- **Библиотеки:**
  - `requests` — HTTP-запросы
  - `beautifulsoup4` — парсинг HTML
  - `lxml` — парсинг XML (sitemap)
  - `python-telegram-bot` — отправка в TG
- **Frontend:** простая HTML-форма (или встроить в статью как iframe)

### Флоу
1. Пользователь вводит URL + Telegram username/chat_id
2. Форма отправляет данные на бэкенд (POST)
3. Парсер запускает все проверки (30-60 сек)
4. Формируется отчёт в Markdown
5. Отчёт отправляется в Telegram пользователю
6. На экране: "✅ Отчёт отправлен в Telegram!"

### Безопасность
- Rate limiting (макс. 5 проверок с одного IP в час)
- Валидация URL (только HTTP/HTTPS, не localhost)
- Timeout на запросы (макс. 10 сек на запрос)

### Хранение данных (опционально)
- База данных для аналитики: какие сайты проверяли, скоры, проблемы
- Для дальнейшего ретаргетинга (email-рассылки, серии сообщений в TG)

---

## Следующие шаги

1. ✅ **Спецификация проверок** (этот документ)
2. ⏭️ **Прототип веб-формы** (HTML + дизайн)
3. ⏭️ **Реализация парсера** (Python-скрипт)
4. ⏭️ **Интеграция с Telegram-ботом**
5. ⏭️ **Тестирование на реальных сайтах** (застройщики + Ida.Lite)
6. ⏭️ **Внедрение CTA в статью**
