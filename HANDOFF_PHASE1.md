# Handoff: SEO Checker MVP - Фаза 1 (Дополнительные проверки)

## Текущее состояние проекта

### ✅ Что уже работает (MVP запущен):

#### 1. Инфраструктура:
- ✅ Backend API (FastAPI + PostgreSQL) на Railway
  - URL: https://thorough-contentment-production-454b.up.railway.app
  - Эндпоинт: `POST /api/check`
  
- ✅ Telegram Bot на Railway
  - Username: @site_SEO_cheker_bot
  - Обработка команд: `/start`, `/help`
  - Обработка прямых URL (включая punycode/кириллицу)
  
- ✅ Frontend (лендинг) на Railway
  - URL: https://ravishing-smile-production-dc59.up.railway.app
  
- ✅ PostgreSQL на Railway (данные проверок)
- ✅ GitHub: https://github.com/asermiajko/seo-checker-mvp

#### 2. Реализованные проверки (6 штук):

**Техническая база:**
1. ✅ **Robots.txt** (`tech-robots`) - проверка User-agent и Sitemap
2. ✅ **Sitemap.xml** (`tech-sitemap`) - валидность и количество URL
3. ✅ **Noindex Check** (`tech-noindex`) - отсутствие noindex на главной
4. ✅ **Analytics** (`tech-analytics`) - Yandex.Metrika, Google Analytics

**Контент:**
5. ✅ **Meta Tags** (`content-meta`) - Title и Description (длина)
6. ✅ **Headings** (`content-headings`) - H1 и структура заголовков

#### 3. Telegram отчёт:
- ✅ Показывает оценку (0-10)
- ✅ Разделяет критичные и важные проблемы
- ✅ Показывает детали каждой проверки (что конкретно не так)
- ✅ Полный список успешных проверок
- ✅ Поддержка punycode доменов

---

## 🎯 Задача для новой сессии: Фаза 1 - Добавить 3 новые проверки

### Что нужно реализовать:

#### 1. **HTML-карта сайта** (важно 🟡)
**ID:** `content-sitemap-html`  
**Что проверяем:**
- Наличие страницы карты сайта для пользователей
- Обычно `/sitemap/`, `/sitemap.html`, `/karta-sajta/`
- Должна содержать ссылки на основные разделы

**Метод:**
```python
# Проверить несколько популярных URL
urls_to_check = [
    f"{site_url}/sitemap/",
    f"{site_url}/sitemap.html", 
    f"{site_url}/karta-sajta/",
    f"{site_url}/map/"
]

for url in urls_to_check:
    response = requests.get(url, timeout=5)
    if response.status_code == 200:
        # Проверить наличие ссылок (минимум 5)
        links = BeautifulSoup(response.content).find_all('a')
        if len(links) >= 5:
            return "ok"
```

**Оценка:**
- ✅ OK: найдена карта с > 5 ссылками
- ⚠️ Важно: карта не найдена

**Сообщение:**
- OK: "✅ HTML-карта найдена (/sitemap/), содержит {N} ссылок"
- Проблема: "⚠️ HTML-карта сайта не найдена (проверены /sitemap/, /karta-sajta/)"

---

#### 2. **OpenGraph теги** (важно 🟡)
**ID:** `content-opengraph`  
**Что проверяем:** На главной странице
- `og:title` - должен быть
- `og:description` - должен быть
- `og:image` - должен быть (URL изображения)

**Метод:**
```python
soup = BeautifulSoup(html_content)

og_title = soup.find('meta', property='og:title')
og_description = soup.find('meta', property='og:description')
og_image = soup.find('meta', property='og:image')

has_title = og_title is not None
has_description = og_description is not None
has_image = og_image is not None

count = sum([has_title, has_description, has_image])
```

**Оценка:**
- ✅ OK: все 3 тега присутствуют
- ⚠️ Важно: отсутствует 1-2 тега
- 🔴 Критично: отсутствуют все теги

**Сообщение:**
- OK: "✅ OpenGraph теги настроены (title, description, image)"
- Проблема: "⚠️ OpenGraph: отсутствуют теги - {список отсутствующих}"

---

#### 3. **Canonical URL** (критично 🔴)
**ID:** `tech-canonical`  
**Что проверяем:** На главной странице
- Наличие тега `<link rel="canonical" href="...">`
- URL должен совпадать с проверяемым сайтом (без дублей)

**Метод:**
```python
soup = BeautifulSoup(html_content)
canonical = soup.find('link', rel='canonical')

if canonical:
    href = canonical.get('href', '')
    # Проверить, что canonical указывает на правильный домен
    parsed_site = urlparse(site_url)
    parsed_canonical = urlparse(href)
    
    if parsed_site.netloc == parsed_canonical.netloc:
        return "ok"
    else:
        return "problem", "canonical указывает на другой домен"
else:
    return "problem", "canonical отсутствует"
```

**Оценка:**
- ✅ OK: canonical присутствует и корректный
- 🔴 Критично: canonical отсутствует или указывает на другой домен

**Сообщение:**
- OK: "✅ Canonical URL установлен корректно"
- Проблема: "❌ Canonical URL отсутствует (может привести к дублям)"

---

## Структура проекта

```
/Users/aleksejsermazko/Documents/Cursor/work/git/projects/seo-checker-tool/
├── backend/
│   ├── app/
│   │   ├── checks/          # ← Здесь реализованы проверки
│   │   │   ├── technical.py  # Robots, Sitemap, Noindex, Analytics
│   │   │   └── content.py    # Meta, Headings
│   │   ├── routes/
│   │   │   └── check.py      # POST /api/check
│   │   ├── schemas.py        # Pydantic схемы ответов
│   │   └── main.py
│   ├── requirements.txt
│   ├── Procfile
│   └── start.sh
├── telegram-bot/
│   ├── bot.py               # Регистрация хэндлеров
│   ├── handlers/
│   │   ├── start.py
│   │   ├── help.py
│   │   └── url.py           # ← Форматирование отчёта
│   ├── services/
│   │   └── api_client.py    # Клиент для backend API
│   └── requirements.txt
└── frontend/
    ├── index.html
    ├── script.js
    └── styles.css
```

---

## План реализации Фазы 1

### Шаг 1: Добавить новые проверки в backend

**Файл:** `backend/app/checks/content.py`

1. Добавить функцию `check_html_sitemap(site_url: str, session: aiohttp.ClientSession)`
2. Добавить функцию `check_opengraph(soup: BeautifulSoup)`
3. Добавить функцию `check_canonical(soup: BeautifulSoup, site_url: str)` в `technical.py`

**Файл:** `backend/app/routes/check.py`

4. Вызвать новые функции в основном эндпоинте
5. Добавить результаты в ответ

### Шаг 2: Обновить схемы ответов (если нужно)

**Файл:** `backend/app/schemas.py`

- Схема `CheckResponseSchema` уже поддерживает динамические проверки через `detailed_checks`
- Изменений не требуется

### Шаг 3: Протестировать локально

```bash
cd backend
source .venv/bin/activate
python -m pytest tests/
```

### Шаг 4: Задеплоить на Railway

```bash
cd backend
railway up
```

### Шаг 5: Протестировать в Telegram

Отправить URL боту @site_SEO_cheker_bot и проверить, что:
- Новые проверки появились в отчёте
- Сообщения корректные
- Оценка пересчитывается правильно

---

## Технические детали

### Backend API Response Structure:

```json
{
  "score": 8.3,
  "problems_critical": 1,
  "problems_important": 2,
  "checks_ok": 6,
  "detailed_checks": [
    {
      "id": "tech-canonical",
      "name": "Canonical URL",
      "status": "ok",
      "message": "✅ Canonical URL установлен корректно",
      "category": "technical",
      "severity": null
    }
  ]
}
```

### Severity уровни:
- `"critical"` - критичные проблемы (🔴)
- `"important"` - важные проблемы (🟡)
- `null` - успешная проверка (✅)

### Telegram Bot форматирование:
- Критичные проблемы показываются первыми
- Важные проблемы - вторым блоком
- Успешные проверки - в конце
- Для каждой проверки: название + детальное сообщение

---

## Полезные ссылки

- **Backend API:** https://thorough-contentment-production-454b.up.railway.app
- **Frontend:** https://ravishing-smile-production-dc59.up.railway.app
- **GitHub:** https://github.com/asermiajko/seo-checker-mvp
- **Railway Project:** https://railway.com/project/b21e4f50-40c2-435f-8bb1-c377355f889f
- **Telegram Bot:** @site_SEO_cheker_bot
- **Спецификация проверок:** `checks-specification-v2-final.md`

---

## Критерии готовности (Definition of Done)

- [ ] Добавлены 3 новые проверки в backend
- [ ] Проверки возвращают корректные результаты
- [ ] Проверки отображаются в Telegram отчёте
- [ ] Оценка (score) пересчитывается с учётом новых проверок
- [ ] Код задеплоен на Railway
- [ ] Протестировано на реальных сайтах застройщиков
- [ ] Коммиты запушены в GitHub

---

## Ожидаемое время выполнения

- Реализация проверок: 30-40 минут
- Тестирование и деплой: 15-20 минут
- **Итого:** 45-60 минут

---

## Следующие фазы (для будущего)

### Фаза 2 (с headless browser):
- Скорость загрузки (FCP)
- Микроразметка Schema.org

### Фаза 3 (с LLM):
- Фильтровые страницы
- Локальное SEO
